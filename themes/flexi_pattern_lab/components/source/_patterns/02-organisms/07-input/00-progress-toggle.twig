{% set uuid = random() %}

<div class="progress-input-container">
  <h4 class="d-inline-block">{{ progressValueLabel|default('Unlimited') }}</h4>
  <p class="text-muted d-inline-block">{{ progressValueProduct|default('Text messages') }}</p>
  <div class="d-flex">
    {% include 'atoms-progress-slider' %}
    {% include 'molecule-toggle-switch' with { uuid: uuid } %}
  </div>
</div>